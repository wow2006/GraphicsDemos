name: build
on: [push]

jobs:
  gcc-release:
    name: GCC Release
    runs-on: ubuntu-latest
    steps:
      - name: Build Docker
        run: pwd && ls && docker build -t test .
      - name: Build code
        run: docker run test:latest
